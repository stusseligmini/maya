# Minimal requirements for basic functionality testing
# This file contains only the core dependencies needed to validate basic imports and structure

# Core Python libraries (these should be available in most environments)
click>=8.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Optional: For basic HTTP testing (if available)
# requests>=2.31.0

# Note: This is a reduced dependency set for environments with limited connectivity
# For full functionality, use requirements.txt